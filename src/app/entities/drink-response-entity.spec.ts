import { DrinkResponseEntity } from './drink-response-entity';

describe('DrinkResponseEntity', () => {
  it('should create an instance', () => {
    expect(new DrinkResponseEntity()).toBeTruthy();
  });
});
